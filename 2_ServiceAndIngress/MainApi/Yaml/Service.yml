apiVersion: v1
kind: Service
metadata:
  name: k8session-service
spec:
  selector:
    app: dotnet-webapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30010
  type: NodePort
